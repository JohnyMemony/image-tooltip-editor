export interface ImageFormData {
    file?: File;
    tooltip: string;
    tooltipPosition: string;
    tooltipColor: string;
}

export interface Image {
    id: string;
    url: string;
    tooltip: string;
    tooltipPosition: string;
    tooltipColor: string;
}
